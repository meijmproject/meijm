/**
 * @desctiption   This file is generated by  code generation tool version 0.2 ^_^
 * @Created   2017-02-13
**/
package com.yh.hr.info.ver.unit.comm.web.action;

import com.yh.hr.info.ver.unit.comm.facade.PbResumeInfoFacade;
import com.yh.hr.info.ver.unit.comm.web.form.PbResumeInfoForm;
import jade.workflow.utils.ObjectUtil;

import java.util.List;
import java.util.Map;

import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

import org.apache.commons.beanutils.BeanUtils;
import org.apache.commons.lang.StringUtils;
import org.apache.struts.action.ActionForm;
import org.apache.struts.action.ActionForward;
import org.apache.struts.action.ActionMapping;

import com.yh.component.taglib.TableTagBean;
import com.yh.hr.res.pb.dto.PbResumeInfoDTO;
import com.yh.platform.core.exception.ServiceException;
import com.yh.platform.core.util.BeanHelper;
import com.yh.platform.core.util.JSONHelper;
import com.yh.platform.core.util.SpringBeanUtil;
import com.yh.platform.core.util.StringUtil;
import com.yh.platform.core.web.action.BaseAction;

public class PbResumeInfoAction extends BaseAction {
	private PbResumeInfoFacade pbResumeInfoFacade = (PbResumeInfoFacade) SpringBeanUtil.getBean("pbResumeInfoFacade");

	/**
	 * 列出所有 PbResumeInfo信息
	 * 
	 * @param mapping
	 * @param form
	 * @param request
	 * @param response
	 * @return
	 * @throws Exception
	 */
	public ActionForward listPbResumeInfo(ActionMapping mapping, ActionForm form, HttpServletRequest request, HttpServletResponse response) throws Exception {
		TableTagBean ttb = TableTagBean.getFromRequest(request);
		if (request.getMethod().equals("POST")) {
	        @SuppressWarnings("rawtypes")
			Map map = BeanUtils.describe(form);
	        ttb.getCondition().copyFrom(map, true);
	    } else {
	        BeanUtils.populate(form, ttb.getCondition());
	        request.setAttribute("pbResumeInfoForm", form);
	    }
        try {
	        List<PbResumeInfoDTO> list = pbResumeInfoFacade.find(ttb);
	        request.setAttribute("list", list);
        } catch (Exception se) {
			this.handleException(request, se, null);
			return mapping.findForward("fail");
		}
        return mapping.findForward("success");
	}
	
	/**
	 * 查找单个PbResumeInfo信息
	 * 
	 * @param mapping
	 * @param form
	 * @param request
	 * @param response
	 * @return
	 * @throws Exception
	 */
	public ActionForward show(ActionMapping mapping, ActionForm form, HttpServletRequest request, HttpServletResponse response) throws Exception {
        String resumeOid = request.getParameter("resumeOid");
        if (StringUtil.isBlank(resumeOid)) {
            resumeOid = ObjectUtil.getValue(request.getAttribute("resumeOid"), String.class);
        }
		try {
            if (StringUtils.isEmpty(resumeOid)) {
				throw new ServiceException("error.pk.invalid", "pbResumeInfo resumeOid is null");
			}
            PbResumeInfoDTO pbResumeInfoDto = pbResumeInfoFacade.get(ObjectUtil.getValue(resumeOid, java.lang.Long.class));
			if (null == pbResumeInfoDto) {
				throw new ServiceException(null, "查询不到相关信息");
			}
			PbResumeInfoForm pbResumeInfoForm = BeanHelper.copyProperties(pbResumeInfoDto, PbResumeInfoForm.class);
			request.setAttribute("pbResumeInfoForm", pbResumeInfoForm);
            request.setAttribute("resumeOid", resumeOid);
		} catch (Exception se) {
			this.handleException(request, se, "resumeOid=" + resumeOid);
			return mapping.getInputForward();
		}
		return mapping.findForward("success");
	}

	/**
	 * 跳转到 PbResumeInfo信息添加页面
	 * 
	 * @param mapping
	 * @param form
	 * @param request
	 * @param response
	 * @return
	 * @throws Exception
	 */
	public ActionForward goCreate(ActionMapping mapping, ActionForm form, HttpServletRequest request, HttpServletResponse response) throws Exception {
		try
		{
			String personOid = request.getParameter("personOid");
			String urlId = request.getParameter("urlId");
			if(StringUtils.isEmpty(personOid))
			{
				throw new ServiceException("personOid is null");
			}
			if(StringUtils.isEmpty(urlId))
			{
				throw new ServiceException("urlId is null");
			}
			request.setAttribute("personOid", personOid);
			request.setAttribute("urlId", urlId);
		}
		catch(Exception se)
		{
			this.handleException(request, se, null);
			return mapping.getInputForward();
		}
		return mapping.findForward(FORWARD_SUCCESS);
	}

	/**
	 * 添加 PbResumeInfo信息
	 * @param mapping
	 * @param form
	 * @param request
	 * @param response
	 * @return
	 * @throws Exception
	 */
	public ActionForward create(ActionMapping mapping, ActionForm form, HttpServletRequest request, HttpServletResponse response) throws Exception {
		if (this.isCancelled(request)) {
			return mapping.findForward("cancel");
		}
		PbResumeInfoForm pbResumeInfoForm = (PbResumeInfoForm) form;
		try {
//			if(StringUtils.isEmpty(${fkOid}))
//			{
//				throw new ServiceException("${fkOid} is null");
//			}
			PbResumeInfoDTO pbResumeInfoDto = BeanHelper.copyProperties(pbResumeInfoForm, PbResumeInfoDTO.class);
//			pbResumeInfoDto.setPersonOid(NumberUtils.longValue(personOid));
			pbResumeInfoFacade.create(pbResumeInfoDto);
			response.getWriter().write(JSONHelper.fromObject(true, null).toString());
		} catch (Exception se) {
			this.handleException(request, se, null);
			response.getWriter().write(JSONHelper.fromObject(false, StringUtils.defaultIfEmpty(se.getMessage(), "修改失败")).toString());
		}

		return null;
	}


	/**
	 * 跳转到 PbResumeInfo信息修改（编辑）页面前的查询回显
	 * 
	 * @param mapping
	 * @param form
	 * @param request
	 * @param response
	 * @return
	 * @throws Exception
	 */
	public ActionForward goUpdate(ActionMapping mapping, ActionForm form, HttpServletRequest request, HttpServletResponse response) throws Exception {
		if(this.isCancelled(request)) {
			return mapping.findForward("cancel");
		}

        String resumeOid = request.getParameter("resumeOid");
        String urlId = request.getParameter("urlId");

		try {
            if (StringUtils.isBlank(resumeOid)) {
				throw new ServiceException("error.pk.invalid", "pbResumeInfo resumeOid is null");
			}
            PbResumeInfoDTO pbResumeInfoDto = pbResumeInfoFacade.get(ObjectUtil.getValue(resumeOid, java.lang.Long.class));
			if (null == pbResumeInfoDto) {
				throw new ServiceException("error.object.notfound", "pbResumeInfo not found by resumeOid");
			}
			PbResumeInfoForm pbResumeInfoForm = BeanHelper.copyProperties(pbResumeInfoDto, PbResumeInfoForm.class);
			request.setAttribute("pbResumeInfoForm", pbResumeInfoForm);
            request.setAttribute("resumeOid", resumeOid);
            request.setAttribute("urlId", urlId);
		} catch (Exception se) {
			this.handleException(request, se, "resumeOid=" + resumeOid);
			return mapping.getInputForward();
		}
		return goUpdateView(mapping, form, request, response);
	}

	/**
	 * 跳转到 PbResumeInfo信息修改（编辑）页面
	 * 
	 * @param mapping
	 * @param form
	 * @param request
	 * @param response
	 * @return
	 * @throws Exception
	 */
    public ActionForward goUpdateView(ActionMapping mapping, ActionForm form, HttpServletRequest request, HttpServletResponse response) throws Exception {
        //add jsp init values
        @SuppressWarnings("unused")
		PbResumeInfoForm pbResumeInfoForm = (PbResumeInfoForm) form;
        return mapping.findForward("success");
    }

	/**
	 * 更新 PbResumeInfo信息
	 * 
	 * @param mapping
	 * @param form
	 * @param request
	 * @param response
	 * @return
	 * @throws Exception
	 */
	public ActionForward update(ActionMapping mapping, ActionForm form, HttpServletRequest request, HttpServletResponse response) throws Exception {
		if(this.isCancelled(request)) {
			return mapping.findForward("cancel");
		}
        String resumeOid = request.getParameter("resumeOid");
		PbResumeInfoForm pbResumeInfoForm = (PbResumeInfoForm) form;
		try {
            if (StringUtils.isBlank(resumeOid)) {
				throw new ServiceException("error.pk.invalid", "pbResumeInfo resumeOid is null");
			}
//			if(StringUtils.isEmpty(${fkOid}))
//			{
//				throw new ServiceException("${fkOid} is null");
//			}
			PbResumeInfoDTO pbResumeInfoDto = new PbResumeInfoDTO();
			BeanHelper.copyProperties(pbResumeInfoForm, pbResumeInfoDto);
			pbResumeInfoFacade.update(pbResumeInfoDto);
			response.getWriter().write(JSONHelper.fromObject(true, null).toString());
		} catch (Exception se) {
			this.handleException(request, se, pbResumeInfoForm);
			response.getWriter().write(JSONHelper.fromObject(false, StringUtils.defaultIfEmpty(se.getMessage(), "修改失败")).toString());
		}

		return null;
	}



	/**
	 * 删除 PbResumeInfo信息
	 * 
	 * @param mapping
	 * @param form
	 * @param request
	 * @param response
	 * @return
	 * @throws Exception
	 */
    public ActionForward delete(ActionMapping mapping, ActionForm form, HttpServletRequest request, HttpServletResponse response) throws Exception {
           String resumeOid = request.getParameter("resumeOid");
        try {
           if(StringUtils.isEmpty(resumeOid))
			{
				throw new ServiceException(null, "resumeOid is null");
			}
            pbResumeInfoFacade.delete(ObjectUtil.getValue(resumeOid, java.lang.Long.class));
            response.getWriter().write(JSONHelper.fromObject(true, null).toString());
        } catch (Exception se) {
            this.handleException(request, se, "resumeOid=" + resumeOid);
            response.getWriter().write(JSONHelper.fromObject(false, StringUtils.defaultIfEmpty(se.getMessage(), "删除失败")).toString());
            return mapping.getInputForward();
        }
        return null;
    }

}
