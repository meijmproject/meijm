/**
 * @desctiption   This file is generated by  code generation tool version 0.2 ^_^
 * @Created   2017-02-13
**/
package com.yh.hr.info.ver.unit.comm.web.action;

import com.yh.hr.info.ver.unit.comm.facade.PbInjuryInfoFacade;
import com.yh.hr.info.ver.unit.comm.web.form.PbInjuryInfoForm;
import jade.workflow.utils.ObjectUtil;

import java.util.List;
import java.util.Map;

import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

import org.apache.commons.beanutils.BeanUtils;
import org.apache.commons.collections.CollectionUtils;
import org.apache.commons.lang.StringUtils;
import org.apache.struts.action.ActionForm;
import org.apache.struts.action.ActionForward;
import org.apache.struts.action.ActionMapping;

import com.yh.component.dictionary.utils.DicHelper;
import com.yh.component.taglib.TableTagBean;
import com.yh.hr.res.dictionary.DicConstants;
import com.yh.hr.res.pb.dto.PbInjuryInfoDTO;
import com.yh.platform.core.exception.ServiceException;
import com.yh.platform.core.util.BeanHelper;
import com.yh.platform.core.util.JSONHelper;
import com.yh.platform.core.util.SpringBeanUtil;
import com.yh.platform.core.util.StringUtil;
import com.yh.platform.core.web.action.BaseAction;

public class PbInjuryInfoAction extends BaseAction {
	private PbInjuryInfoFacade pbInjuryInfoFacade = (PbInjuryInfoFacade) SpringBeanUtil.getBean("pbInjuryInfoFacade");

	
	//dto属性字典转换
	private void dtoDicMap(PbInjuryInfoDTO dto) throws ServiceException {
		dto.setHurtLevel(DicHelper.viewName(DicConstants.YHRS0087,dto.getHurtLevel()));
	}
	/**
	 * 列出所有 PbInjuryInfo信息
	 * 
	 * @param mapping
	 * @param form
	 * @param request
	 * @param response
	 * @return
	 * @throws Exception
	 */
	public ActionForward listPbInjuryInfo(ActionMapping mapping, ActionForm form, HttpServletRequest request, HttpServletResponse response) throws Exception {
		TableTagBean ttb = TableTagBean.getFromRequest(request);
		if (request.getMethod().equals("POST")) {
	        @SuppressWarnings("rawtypes")
			Map map = BeanUtils.describe(form);
	        ttb.getCondition().copyFrom(map, true);
	    } else {
	        BeanUtils.populate(form, ttb.getCondition());
	        request.setAttribute("pbInjuryInfoForm", form);
	    }
        try {
        	List<PbInjuryInfoDTO> list = pbInjuryInfoFacade.find(ttb);
	        if(CollectionUtils.isNotEmpty(list)){
		        for(PbInjuryInfoDTO dto : list){
		        	dtoDicMap(dto);
		        }
		        request.setAttribute("list", list);
	        }
        } catch (Exception se) {
			this.handleException(request, se, null);
			return mapping.findForward("fail");
		}
        return mapping.findForward("success");
	}
	
	/**
	 * 查找单个PbInjuryInfo信息
	 * 
	 * @param mapping
	 * @param form
	 * @param request
	 * @param response
	 * @return
	 * @throws Exception
	 */
	public ActionForward show(ActionMapping mapping, ActionForm form, HttpServletRequest request, HttpServletResponse response) throws Exception {
        String injuryOid = request.getParameter("injuryOid");
        if (StringUtil.isBlank(injuryOid)) {
            injuryOid = ObjectUtil.getValue(request.getAttribute("injuryOid"), String.class);
        }
		try {
            if (StringUtils.isEmpty(injuryOid)) {
				throw new ServiceException("error.pk.invalid", "pbInjuryInfo injuryOid is null");
			}
            PbInjuryInfoDTO pbInjuryInfoDto = pbInjuryInfoFacade.get(ObjectUtil.getValue(injuryOid, java.lang.Long.class));
			if (null == pbInjuryInfoDto) {
				throw new ServiceException(null, "查询不到相关信息");
			}
			dtoDicMap(pbInjuryInfoDto);
			PbInjuryInfoForm pbInjuryInfoForm = BeanHelper.copyProperties(pbInjuryInfoDto, PbInjuryInfoForm.class);
			request.setAttribute("pbInjuryInfoForm", pbInjuryInfoForm);
            request.setAttribute("injuryOid", injuryOid);
		} catch (Exception se) {
			this.handleException(request, se, "injuryOid=" + injuryOid);
			return mapping.getInputForward();
		}
		return mapping.findForward("success");
	}

	/**
	 * 跳转到 PbInjuryInfo信息添加页面
	 * 
	 * @param mapping
	 * @param form
	 * @param request
	 * @param response
	 * @return
	 * @throws Exception
	 */
	public ActionForward goCreate(ActionMapping mapping, ActionForm form, HttpServletRequest request, HttpServletResponse response) throws Exception {
		try
		{
			String personOid = request.getParameter("personOid");
			String urlId = request.getParameter("urlId");
			if(StringUtils.isEmpty(personOid))
			{
				throw new ServiceException("personOid is null");
			}
			if(StringUtils.isEmpty(urlId))
			{
				throw new ServiceException("urlId is null");
			}
			request.setAttribute("personOid", personOid);
			request.setAttribute("urlId", urlId);
		}
		catch(Exception se)
		{
			this.handleException(request, se, null);
			return mapping.getInputForward();
		}
		return mapping.findForward(FORWARD_SUCCESS);
	}

	/**
	 * 添加 PbInjuryInfo信息
	 * @param mapping
	 * @param form
	 * @param request
	 * @param response
	 * @return
	 * @throws Exception
	 */
	public ActionForward create(ActionMapping mapping, ActionForm form, HttpServletRequest request, HttpServletResponse response) throws Exception {
		if (this.isCancelled(request)) {
			return mapping.findForward("cancel");
		}
		PbInjuryInfoForm pbInjuryInfoForm = (PbInjuryInfoForm) form;
		try {
//			if(StringUtils.isEmpty(${fkOid}))
//			{
//				throw new ServiceException("${fkOid} is null");
//			}
			PbInjuryInfoDTO pbInjuryInfoDto = BeanHelper.copyProperties(pbInjuryInfoForm, PbInjuryInfoDTO.class);
//			pbInjuryInfoDto.setPersonOid(NumberUtils.longValue(personOid));
			pbInjuryInfoFacade.create(pbInjuryInfoDto);
			response.getWriter().write(JSONHelper.fromObject(true, null).toString());
		} catch (Exception se) {
			this.handleException(request, se, null);
			response.getWriter().write(JSONHelper.fromObject(false, StringUtils.defaultIfEmpty(se.getMessage(), "修改失败")).toString());
		}

		return null;
	}


	/**
	 * 跳转到 PbInjuryInfo信息修改（编辑）页面前的查询回显
	 * 
	 * @param mapping
	 * @param form
	 * @param request
	 * @param response
	 * @return
	 * @throws Exception
	 */
	public ActionForward goUpdate(ActionMapping mapping, ActionForm form, HttpServletRequest request, HttpServletResponse response) throws Exception {
		if(this.isCancelled(request)) {
			return mapping.findForward("cancel");
		}

        String injuryOid = request.getParameter("injuryOid");
        String urlId = request.getParameter("urlId");

		try {
            if (StringUtils.isBlank(injuryOid)) {
				throw new ServiceException("error.pk.invalid", "pbInjuryInfo injuryOid is null");
			}
            PbInjuryInfoDTO pbInjuryInfoDto = pbInjuryInfoFacade.get(ObjectUtil.getValue(injuryOid, java.lang.Long.class));
			if (null == pbInjuryInfoDto) {
				throw new ServiceException("error.object.notfound", "pbInjuryInfo not found by injuryOid");
			}
			PbInjuryInfoForm pbInjuryInfoForm = BeanHelper.copyProperties(pbInjuryInfoDto, PbInjuryInfoForm.class);
			request.setAttribute("pbInjuryInfoForm", pbInjuryInfoForm);
            request.setAttribute("injuryOid", injuryOid);
            request.setAttribute("urlId", urlId);
		} catch (Exception se) {
			this.handleException(request, se, "injuryOid=" + injuryOid);
			return mapping.getInputForward();
		}
		return goUpdateView(mapping, form, request, response);
	}

	/**
	 * 跳转到 PbInjuryInfo信息修改（编辑）页面
	 * 
	 * @param mapping
	 * @param form
	 * @param request
	 * @param response
	 * @return
	 * @throws Exception
	 */
    public ActionForward goUpdateView(ActionMapping mapping, ActionForm form, HttpServletRequest request, HttpServletResponse response) throws Exception {
        //add jsp init values
        @SuppressWarnings("unused")
		PbInjuryInfoForm pbInjuryInfoForm = (PbInjuryInfoForm) form;
        return mapping.findForward("success");
    }

	/**
	 * 更新 PbInjuryInfo信息
	 * 
	 * @param mapping
	 * @param form
	 * @param request
	 * @param response
	 * @return
	 * @throws Exception
	 */
	public ActionForward update(ActionMapping mapping, ActionForm form, HttpServletRequest request, HttpServletResponse response) throws Exception {
		if(this.isCancelled(request)) {
			return mapping.findForward("cancel");
		}
        String injuryOid = request.getParameter("injuryOid");
		PbInjuryInfoForm pbInjuryInfoForm = (PbInjuryInfoForm) form;
		try {
            if (StringUtils.isBlank(injuryOid)) {
				throw new ServiceException("error.pk.invalid", "pbInjuryInfo injuryOid is null");
			}
//			if(StringUtils.isEmpty(${fkOid}))
//			{
//				throw new ServiceException("${fkOid} is null");
//			}
			PbInjuryInfoDTO pbInjuryInfoDto = new PbInjuryInfoDTO();
			BeanHelper.copyProperties(pbInjuryInfoForm, pbInjuryInfoDto);
			pbInjuryInfoFacade.update(pbInjuryInfoDto);
			response.getWriter().write(JSONHelper.fromObject(true, null).toString());
		} catch (Exception se) {
			this.handleException(request, se, pbInjuryInfoForm);
			response.getWriter().write(JSONHelper.fromObject(false, StringUtils.defaultIfEmpty(se.getMessage(), "修改失败")).toString());
		}

		return null;
	}



	/**
	 * 删除 PbInjuryInfo信息
	 * 
	 * @param mapping
	 * @param form
	 * @param request
	 * @param response
	 * @return
	 * @throws Exception
	 */
    public ActionForward delete(ActionMapping mapping, ActionForm form, HttpServletRequest request, HttpServletResponse response) throws Exception {
           String injuryOid = request.getParameter("injuryOid");
        try {
           if(StringUtils.isEmpty(injuryOid))
			{
				throw new ServiceException(null, "injuryOid is null");
			}
            pbInjuryInfoFacade.delete(ObjectUtil.getValue(injuryOid, java.lang.Long.class));
            response.getWriter().write(JSONHelper.fromObject(true, null).toString());
        } catch (Exception se) {
            this.handleException(request, se, "injuryOid=" + injuryOid);
            return mapping.getInputForward();
        }
        return null;
    }

}
