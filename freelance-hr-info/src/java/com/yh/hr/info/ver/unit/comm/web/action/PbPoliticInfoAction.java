/**
 * @desctiption   This file is generated by  code generation tool version 0.2 ^_^
 * @Created   2017-02-13
**/
package com.yh.hr.info.ver.unit.comm.web.action;

import com.yh.hr.info.ver.unit.comm.web.form.PbPoliticInfoForm;
import jade.workflow.utils.ObjectUtil;

import java.util.Map;

import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

import org.apache.commons.beanutils.BeanUtils;
import org.apache.commons.lang.StringUtils;
import org.apache.struts.action.ActionForm;
import org.apache.struts.action.ActionForward;
import org.apache.struts.action.ActionMapping;

import com.yh.component.taglib.TableTagBean;
import com.yh.hr.info.ver.unit.comm.facade.PbPoliticInfoFacade;
import com.yh.hr.res.pb.dto.PbPoliticInfoDTO;
import com.yh.platform.core.exception.ServiceException;
import com.yh.platform.core.util.BeanHelper;
import com.yh.platform.core.util.DateUtil;
import com.yh.platform.core.util.JSONHelper;
import com.yh.platform.core.util.NumberUtils;
import com.yh.platform.core.util.SpringBeanUtil;
import com.yh.platform.core.util.StringUtil;
import com.yh.platform.core.web.UserContext;
import com.yh.platform.core.web.action.BaseAction;

public class PbPoliticInfoAction extends BaseAction {
	private PbPoliticInfoFacade pbPoliticInfoFacade = (PbPoliticInfoFacade) SpringBeanUtil.getBean("pbPoliticInfoFacade");

	/**
	 * 列出所有 PbPoliticInfo信息
	 * 
	 * @param mapping
	 * @param form
	 * @param request
	 * @param response
	 * @return
	 * @throws Exception
	 */
	public ActionForward listPbPoliticInfo(ActionMapping mapping, ActionForm form, HttpServletRequest request, HttpServletResponse response) throws Exception {
		TableTagBean ttb = TableTagBean.getFromRequest(request);
		if (request.getMethod().equals("POST")) {
	        @SuppressWarnings("rawtypes")
			Map map = BeanUtils.describe(form);
	        ttb.getCondition().copyFrom(map, true);
	    } else {
	        BeanUtils.populate(form, ttb.getCondition());
	        request.setAttribute("pbPoliticInfoForm", form);
	    }
        try {
	        pbPoliticInfoFacade.find(ttb);
	        request.setAttribute("list", ttb.getList());
        } catch (Exception se) {
			this.handleException(request, se, null);
			return mapping.findForward("fail");
		}
        return mapping.findForward("success");
	}
	
	/**
	 * 查找单个PbPoliticInfo信息
	 * 
	 * @param mapping
	 * @param form
	 * @param request
	 * @param response
	 * @return
	 * @throws Exception
	 */
	public ActionForward show(ActionMapping mapping, ActionForm form, HttpServletRequest request, HttpServletResponse response) throws Exception {
        String politicOid = request.getParameter("politicOid");
        if (StringUtil.isBlank(politicOid)) {
            politicOid = ObjectUtil.getValue(request.getAttribute("politicOid"), String.class);
        }
		try {	
            if (StringUtils.isEmpty(politicOid)) {
				throw new ServiceException("error.pk.invalid", "pbPoliticInfo politicOid is null");
			}
            PbPoliticInfoDTO pbPoliticInfoDto = pbPoliticInfoFacade.get(ObjectUtil.getValue(politicOid, java.lang.Long.class));
			if (null == pbPoliticInfoDto) {
				throw new ServiceException(null, "查询不到相关信息");
			}
			PbPoliticInfoForm pbPoliticInfoForm = BeanHelper.copyProperties(pbPoliticInfoDto, PbPoliticInfoForm.class);
			request.setAttribute("pbPoliticInfoForm", pbPoliticInfoForm);
            request.setAttribute("politicOid", politicOid);
		} catch (Exception se) {
			this.handleException(request, se, "politicOid=" + politicOid);
			return mapping.getInputForward();
		}
		return mapping.findForward("success");
	}

	/**
	 * 跳转到 PbPoliticInfo信息添加页面
	 * 
	 * @param mapping
	 * @param form
	 * @param request
	 * @param response
	 * @return
	 * @throws Exception
	 */
	public ActionForward goCreate(ActionMapping mapping, ActionForm form, HttpServletRequest request, HttpServletResponse response) throws Exception {
		//参数校验，可根据实际情况修改
		String urlId = request.getParameter("urlId");
		String personOid = request.getParameter("personOid");
		try
		{
			if(StringUtils.isEmpty(personOid))
			{
				throw new ServiceException("personOid is null");
			}
			request.setAttribute("personOid", personOid);
			request.setAttribute("urlId", urlId);
		}
		catch(Exception se)
		{
			this.handleException(request, se, null);
			return mapping.getInputForward();
		}
		return mapping.findForward(FORWARD_SUCCESS);
	}

	/**
	 * 添加 PbPoliticInfo信息
	 * @param mapping
	 * @param form
	 * @param request
	 * @param response
	 * @return
	 * @throws Exception
	 */
	public ActionForward create(ActionMapping mapping, ActionForm form, HttpServletRequest request, HttpServletResponse response) throws Exception {
		if (this.isCancelled(request)) {
			return mapping.findForward("cancel");
		}
		String personOid = request.getParameter("personOid");
		PbPoliticInfoForm pbPoliticInfoForm = (PbPoliticInfoForm) form;
		try {
			if(StringUtils.isEmpty(personOid))
			{
				throw new ServiceException("personOid is null");
			}
			if(StringUtils.isNotEmpty(pbPoliticInfoForm.getOutPartyDateStr())){
			if(DateUtil.parseDate(pbPoliticInfoForm.getOutPartyDateStr()).getTime()>DateUtil.now().getTime()){
				 throw new ServiceException(null, "离党日期不能晚于今天");
			 }
			}
			PbPoliticInfoDTO pbPoliticInfoDto = BeanHelper.copyProperties(pbPoliticInfoForm, PbPoliticInfoDTO.class);
			pbPoliticInfoDto.setPersonOid(NumberUtils.longValue(personOid));
			pbPoliticInfoDto.setCreateBy(UserContext.getLoginUserID());
			pbPoliticInfoDto.setCreateName(UserContext.getLoginUserName());
			pbPoliticInfoDto.setCreateDate(DateUtil.now());
			pbPoliticInfoFacade.create(pbPoliticInfoDto);
			response.getWriter().write(JSONHelper.fromObject(true, null).toString());
		} catch (Exception se) {
			this.handleException(request, se, null);
			response.getWriter().write(JSONHelper.fromObject(false, StringUtils.defaultIfEmpty(se.getMessage(), "修改失败")).toString());
		}

		return null;
	}


	/**
	 * 跳转到 PbPoliticInfo信息修改（编辑）页面前的查询回显
	 * 
	 * @param mapping
	 * @param form
	 * @param request
	 * @param response
	 * @return
	 * @throws Exception
	 */
	public ActionForward goUpdate(ActionMapping mapping, ActionForm form, HttpServletRequest request, HttpServletResponse response) throws Exception {
		if(this.isCancelled(request)) {
			return mapping.findForward("cancel");
		}

		String urlId = request.getParameter("urlId");
        String politicOid = request.getParameter("politicOid");

		try {
            if (StringUtils.isBlank(politicOid)) {
				throw new ServiceException("error.pk.invalid", "pbPoliticInfo politicOid is null");
			}
            PbPoliticInfoDTO pbPoliticInfoDto = pbPoliticInfoFacade.get(ObjectUtil.getValue(politicOid, java.lang.Long.class));
			if (null == pbPoliticInfoDto) {
				throw new ServiceException("error.object.notfound", "pbPoliticInfo not found by politicOid");
			}
			PbPoliticInfoForm pbPoliticInfoForm = BeanHelper.copyProperties(pbPoliticInfoDto, PbPoliticInfoForm.class);
			request.setAttribute("pbPoliticInfoForm", pbPoliticInfoForm);
            request.setAttribute("urlId", urlId);
		} catch (Exception se) {
			this.handleException(request, se, "politicOid=" + politicOid);
			return mapping.getInputForward();
		}
		return goUpdateView(mapping, form, request, response);
	}

	/**
	 * 跳转到 PbPoliticInfo信息修改（编辑）页面
	 * 
	 * @param mapping
	 * @param form
	 * @param request
	 * @param response
	 * @return
	 * @throws Exception
	 */
    public ActionForward goUpdateView(ActionMapping mapping, ActionForm form, HttpServletRequest request, HttpServletResponse response) throws Exception {
        //add jsp init values
        @SuppressWarnings("unused")
		PbPoliticInfoForm pbPoliticInfoForm = (PbPoliticInfoForm) form;
        return mapping.findForward("success");
    }

	/**
	 * 更新 PbPoliticInfo信息
	 * 
	 * @param mapping
	 * @param form
	 * @param request
	 * @param response
	 * @return
	 * @throws Exception
	 */
	public ActionForward update(ActionMapping mapping, ActionForm form, HttpServletRequest request, HttpServletResponse response) throws Exception {
		if(this.isCancelled(request)) {
			return mapping.findForward("cancel");
		}
		PbPoliticInfoForm pbPoliticInfoForm = (PbPoliticInfoForm) form;
		try {
			if(StringUtils.isNotEmpty(pbPoliticInfoForm.getOutPartyDateStr())){
			 if(DateUtil.parseDate(pbPoliticInfoForm.getOutPartyDateStr()).getTime()>DateUtil.now().getTime()){
				 throw new ServiceException(null, "离党日期不能晚于今天");
			 }
			}
			PbPoliticInfoDTO pbPoliticInfoDto = new PbPoliticInfoDTO();
			BeanHelper.copyProperties(pbPoliticInfoForm, pbPoliticInfoDto);
			pbPoliticInfoDto.setUpdateBy(UserContext.getLoginUserID());
			pbPoliticInfoDto.setUpdateName(UserContext.getLoginUserName());
			pbPoliticInfoDto.setUpdateDate(DateUtil.now());
			pbPoliticInfoFacade.update(pbPoliticInfoDto);
			response.getWriter().write(JSONHelper.fromObject(true, null).toString());
		} catch (Exception se) {
			this.handleException(request, se, pbPoliticInfoForm);
			response.getWriter().write(JSONHelper.fromObject(false, StringUtils.defaultIfEmpty(se.getMessage(), "修改失败")).toString());
		}

		return null;
	}



	/**
	 * 删除 PbPoliticInfo信息
	 * 
	 * @param mapping
	 * @param form
	 * @param request
	 * @param response
	 * @return
	 * @throws Exception
	 */
    public ActionForward delete(ActionMapping mapping, ActionForm form, HttpServletRequest request, HttpServletResponse response) throws Exception {
           String politicOid = request.getParameter("politicOid");
        try {
           if(StringUtils.isEmpty(politicOid))
			{
				throw new ServiceException(null, "politicOid is null");
			}
            pbPoliticInfoFacade.delete(ObjectUtil.getValue(politicOid, java.lang.Long.class));
            response.getWriter().write(JSONHelper.fromObject(true, null).toString());
        } catch (Exception se) {
            this.handleException(request, se, "politicOid=" + politicOid);
            return mapping.getInputForward();
        }
        return null;
    }

}
