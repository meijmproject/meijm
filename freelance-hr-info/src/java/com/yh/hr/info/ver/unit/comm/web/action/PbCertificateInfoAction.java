/**
 * @desctiption   This file is generated by  code generation tool version 0.2 ^_^
 * @Created   2017-02-10
**/
package com.yh.hr.info.ver.unit.comm.web.action;

import jade.workflow.utils.ObjectUtil;

import java.util.List;
import java.util.Map;

import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

import org.apache.commons.beanutils.BeanUtils;
import org.apache.commons.collections.CollectionUtils;
import org.apache.commons.lang.StringUtils;
import org.apache.struts.action.ActionForm;
import org.apache.struts.action.ActionForward;
import org.apache.struts.action.ActionMapping;

import com.yh.component.dictionary.utils.DicHelper;
import com.yh.component.taglib.TableTagBean;
import com.yh.hr.info.ver.unit.comm.facade.PbCertificateInfoFacade;
import com.yh.hr.info.ver.unit.comm.web.form.PbCertificateInfoForm;
import com.yh.hr.res.dictionary.DicConstants;
import com.yh.hr.res.pb.dto.PbCertificateInfoDTO;
import com.yh.platform.core.exception.ServiceException;
import com.yh.platform.core.util.BeanHelper;
import com.yh.platform.core.util.JSONHelper;
import com.yh.platform.core.util.SpringBeanUtil;
import com.yh.platform.core.util.StringUtil;
import com.yh.platform.core.web.action.BaseAction;

public class PbCertificateInfoAction extends BaseAction {
	private PbCertificateInfoFacade pbCertificateInfoFacade = (PbCertificateInfoFacade) SpringBeanUtil.getBean("pbCertificateInfoFacade");

	//dto属性字典转换
	private void dtoDicMap(PbCertificateInfoDTO dto) throws ServiceException {
		dto.setCancelFlag(DicHelper.viewName(DicConstants.YHRS0003,dto.getCancelFlag()));
	}
	/**
	 * 列出所有 PbCertificateInfo信息
	 * 
	 * @param mapping
	 * @param form
	 * @param request
	 * @param response
	 * @return
	 * @throws Exception
	 */
	public ActionForward listPbCertificateInfo(ActionMapping mapping, ActionForm form, HttpServletRequest request, HttpServletResponse response) throws Exception {
		TableTagBean ttb = TableTagBean.getFromRequest(request);
		if (request.getMethod().equals("POST")) {
	        @SuppressWarnings("rawtypes")
			Map map = BeanUtils.describe(form);
	        ttb.getCondition().copyFrom(map, true);
	    } else {
	        BeanUtils.populate(form, ttb.getCondition());
	        request.setAttribute("pbCertificateInfoForm", form);
	    }
        try {
        	List<PbCertificateInfoDTO> list = pbCertificateInfoFacade.find(ttb);
	        if(CollectionUtils.isNotEmpty(list)){
		        for(PbCertificateInfoDTO dto : list){
		        	dtoDicMap(dto);
		        }
		        request.setAttribute("list", list);
	        }
        } catch (Exception se) {
			this.handleException(request, se, null);
			return mapping.findForward("fail");
		}
        return mapping.findForward("success");
	}
	
	/**
	 * 查找单个PbCertificateInfo信息
	 * 
	 * @param mapping
	 * @param form
	 * @param request
	 * @param response
	 * @return
	 * @throws Exception
	 */
	public ActionForward show(ActionMapping mapping, ActionForm form, HttpServletRequest request, HttpServletResponse response) throws Exception {
        String certificateOid = request.getParameter("certificateOid");
        if (StringUtil.isBlank(certificateOid)) {
            certificateOid = ObjectUtil.getValue(request.getAttribute("certificateOid"), String.class);
        }
		try {
            if (StringUtils.isEmpty(certificateOid)) {
				throw new ServiceException("error.pk.invalid", "pbCertificateInfo certificateOid is null");
			}
            PbCertificateInfoDTO pbCertificateInfoDto = pbCertificateInfoFacade.get(ObjectUtil.getValue(certificateOid, java.lang.Long.class));
			if (null == pbCertificateInfoDto) {
				throw new ServiceException(null, "查询不到相关信息");
			}
			dtoDicMap(pbCertificateInfoDto);
			PbCertificateInfoForm pbCertificateInfoForm = BeanHelper.copyProperties(pbCertificateInfoDto, PbCertificateInfoForm.class);
			request.setAttribute("pbCertificateInfoForm", pbCertificateInfoForm);
            request.setAttribute("certificateOid", certificateOid);
		} catch (Exception se) {
			this.handleException(request, se, "certificateOid=" + certificateOid);
			return mapping.getInputForward();
		}
		return mapping.findForward("success");
	}

	/**
	 * 跳转到 PbCertificateInfo信息添加页面
	 * 
	 * @param mapping
	 * @param form
	 * @param request
	 * @param response
	 * @return
	 * @throws Exception
	 */
	public ActionForward goCreate(ActionMapping mapping, ActionForm form, HttpServletRequest request, HttpServletResponse response) throws Exception {
		//参数校验，可根据实际情况修改
		try
		{
			String personOid = request.getParameter("personOid");
			String urlId = request.getParameter("urlId");
			if(StringUtils.isEmpty(personOid))
			{
				throw new ServiceException("personOid is null");
			}
			if(StringUtils.isEmpty(urlId))
			{
				throw new ServiceException("urlId is null");
			}
			request.setAttribute("personOid", personOid);
			request.setAttribute("urlId", urlId);
		}
		catch(Exception se)
		{
			this.handleException(request, se, null);
			return mapping.getInputForward();
		}
		return mapping.findForward(FORWARD_SUCCESS);
	}

	/**
	 * 添加 PbCertificateInfo信息
	 * @param mapping
	 * @param form
	 * @param request
	 * @param response
	 * @return
	 * @throws Exception
	 */
	public ActionForward create(ActionMapping mapping, ActionForm form, HttpServletRequest request, HttpServletResponse response) throws Exception {
		if (this.isCancelled(request)) {
			return mapping.findForward("cancel");
		}
		PbCertificateInfoForm pbCertificateInfoForm = (PbCertificateInfoForm) form;
		try {
			TableTagBean ttb = TableTagBean.getFromRequest(request);
			List<PbCertificateInfoDTO> list = pbCertificateInfoFacade.find(ttb);
			if(list!=null && list.size()>0) {
				for(PbCertificateInfoDTO dto: list) {
					if(dto!=null && pbCertificateInfoForm!=null && dto.getCertificateNo().equals(pbCertificateInfoForm.getCertificateNo())) {
						throw new Exception("执业注册证书编号已存在！");
					}
				}
			}
			PbCertificateInfoDTO pbCertificateInfoDto = BeanHelper.copyProperties(pbCertificateInfoForm, PbCertificateInfoDTO.class);
//			pbCertificateInfoDto.setPersonOid(NumberUtils.longValue(personOid));
			pbCertificateInfoFacade.create(pbCertificateInfoDto);
			response.getWriter().write(JSONHelper.fromObject(true, null).toString());
		} catch (Exception se) {
			this.handleException(request, se, null);
			response.getWriter().write(JSONHelper.fromObject(false, StringUtils.defaultIfEmpty(se.getMessage(), "修改失败")).toString());
		}

		return null;
	}


	/**
	 * 跳转到 PbCertificateInfo信息修改（编辑）页面前的查询回显
	 * 
	 * @param mapping
	 * @param form
	 * @param request
	 * @param response
	 * @return
	 * @throws Exception
	 */
	public ActionForward goUpdate(ActionMapping mapping, ActionForm form, HttpServletRequest request, HttpServletResponse response) throws Exception {
		if(this.isCancelled(request)) {
			return mapping.findForward("cancel");
		}

        String certificateOid = request.getParameter("certificateOid");
        String urlId = request.getParameter("urlId");

		try {
            if (StringUtils.isBlank(certificateOid)) {
				throw new ServiceException("error.pk.invalid", "pbCertificateInfo certificateOid is null");
			}
            PbCertificateInfoDTO pbCertificateInfoDto = pbCertificateInfoFacade.get(ObjectUtil.getValue(certificateOid, java.lang.Long.class));
			if (null == pbCertificateInfoDto) {
				throw new ServiceException("error.object.notfound", "pbCertificateInfo not found by certificateOid");
			}
			PbCertificateInfoForm pbCertificateInfoForm = BeanHelper.copyProperties(pbCertificateInfoDto, PbCertificateInfoForm.class);
			request.setAttribute("pbCertificateInfoForm", pbCertificateInfoForm);
            request.setAttribute("certificateOid", certificateOid);
            request.setAttribute("urlId", urlId);
		} catch (Exception se) {
			this.handleException(request, se, "certificateOid=" + certificateOid);
			return mapping.getInputForward();
		}
		return goUpdateView(mapping, form, request, response);
	}

	/**
	 * 跳转到 PbCertificateInfo信息修改（编辑）页面
	 * 
	 * @param mapping
	 * @param form
	 * @param request
	 * @param response
	 * @return
	 * @throws Exception
	 */
    public ActionForward goUpdateView(ActionMapping mapping, ActionForm form, HttpServletRequest request, HttpServletResponse response) throws Exception {
        //add jsp init values
        @SuppressWarnings("unused")
		PbCertificateInfoForm pbCertificateInfoForm = (PbCertificateInfoForm) form;
        return mapping.findForward("success");
    }

	/**
	 * 更新 PbCertificateInfo信息
	 * 
	 * @param mapping
	 * @param form
	 * @param request
	 * @param response
	 * @return
	 * @throws Exception
	 */
	public ActionForward update(ActionMapping mapping, ActionForm form, HttpServletRequest request, HttpServletResponse response) throws Exception {
		if(this.isCancelled(request)) {
			return mapping.findForward("cancel");
		}
        String certificateOid = request.getParameter("certificateOid");
		PbCertificateInfoForm pbCertificateInfoForm = (PbCertificateInfoForm) form;
		try {
            if (StringUtils.isBlank(certificateOid)) {
				throw new ServiceException("error.pk.invalid", "pbCertificateInfo certificateOid is null");
			}
//			if(StringUtils.isEmpty(${fkOid}))
//			{
//				throw new ServiceException("${fkOid} is null");
//			}
			PbCertificateInfoDTO pbCertificateInfoDto = new PbCertificateInfoDTO();
			BeanHelper.copyProperties(pbCertificateInfoForm, pbCertificateInfoDto);
			pbCertificateInfoFacade.update(pbCertificateInfoDto);
			response.getWriter().write(JSONHelper.fromObject(true, null).toString());
		} catch (Exception se) {
			this.handleException(request, se, pbCertificateInfoForm);
			response.getWriter().write(JSONHelper.fromObject(false, StringUtils.defaultIfEmpty(se.getMessage(), "修改失败")).toString());
		}

		return null;
	}



	/**
	 * 删除 PbCertificateInfo信息
	 * 
	 * @param mapping
	 * @param form
	 * @param request
	 * @param response
	 * @return
	 * @throws Exception
	 */
    public ActionForward delete(ActionMapping mapping, ActionForm form, HttpServletRequest request, HttpServletResponse response) throws Exception {
           String certificateOid = request.getParameter("certificateOid");
        try {
           if(StringUtils.isEmpty(certificateOid))
			{
				throw new ServiceException(null, "certificateOid is null");
			}
            pbCertificateInfoFacade.delete(ObjectUtil.getValue(certificateOid, java.lang.Long.class));
            response.getWriter().write(JSONHelper.fromObject(true, null).toString());
        } catch (Exception se) {
            this.handleException(request, se, "certificateOid=" + certificateOid);
            return mapping.getInputForward();
        }
        return null;
    }

}
