/**
 * @desctiption   This file is generated by  code generation tool version 0.2 ^_^
 * @Created   2017-02-13
**/
package com.yh.hr.info.ver.unit.comm.web.action;

import com.yh.hr.info.ver.unit.comm.facade.PbRewardInfoFacade;
import com.yh.hr.info.ver.unit.comm.web.form.PbRewardInfoForm;
import jade.workflow.utils.ObjectUtil;

import java.util.Map;

import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

import org.apache.commons.beanutils.BeanUtils;
import org.apache.commons.lang.StringUtils;
import org.apache.struts.action.ActionForm;
import org.apache.struts.action.ActionForward;
import org.apache.struts.action.ActionMapping;

import com.yh.component.taglib.TableTagBean;
import com.yh.hr.res.pb.dto.PbRewardInfoDTO;
import com.yh.platform.core.exception.ServiceException;
import com.yh.platform.core.util.BeanHelper;
import com.yh.platform.core.util.DateUtil;
import com.yh.platform.core.util.JSONHelper;
import com.yh.platform.core.util.NumberUtils;
import com.yh.platform.core.util.SpringBeanUtil;
import com.yh.platform.core.util.StringUtil;
import com.yh.platform.core.web.UserContext;
import com.yh.platform.core.web.action.BaseAction;

public class PbRewardInfoAction extends BaseAction {
	private PbRewardInfoFacade pbRewardInfoFacade = (PbRewardInfoFacade) SpringBeanUtil.getBean("pbRewardInfoFacade");

	/**
	 * 列出所有 PbRewardInfo信息
	 * 
	 * @param mapping
	 * @param form
	 * @param request
	 * @param response
	 * @return
	 * @throws Exception
	 */
	public ActionForward listPbRewardInfo(ActionMapping mapping, ActionForm form, HttpServletRequest request, HttpServletResponse response) throws Exception {
		TableTagBean ttb = TableTagBean.getFromRequest(request);
		if (request.getMethod().equals("POST")) {
	        @SuppressWarnings("rawtypes")
			Map map = BeanUtils.describe(form);
	        ttb.getCondition().copyFrom(map, true);
	    } else {
	        BeanUtils.populate(form, ttb.getCondition());
	        request.setAttribute("pbRewardInfoForm", form);
	    }
        try {
	        pbRewardInfoFacade.find(ttb);
	        request.setAttribute("list", ttb.getList());
        } catch (Exception se) {
			this.handleException(request, se, null);
			return mapping.findForward("fail");
		}
        return mapping.findForward("success");
	}
	
	/**
	 * 查找单个PbRewardInfo信息
	 * 
	 * @param mapping
	 * @param form
	 * @param request
	 * @param response
	 * @return
	 * @throws Exception
	 */
	public ActionForward show(ActionMapping mapping, ActionForm form, HttpServletRequest request, HttpServletResponse response) throws Exception {
        String rewardOid = request.getParameter("rewardOid");
        if (StringUtil.isBlank(rewardOid)) {
            rewardOid = ObjectUtil.getValue(request.getAttribute("rewardOid"), String.class);
        }
		try {
            if (StringUtils.isEmpty(rewardOid)) {
				throw new ServiceException("error.pk.invalid", "pbRewardInfo rewardOid is null");
			}
            PbRewardInfoDTO pbRewardInfoDto = pbRewardInfoFacade.get(ObjectUtil.getValue(rewardOid, java.lang.Long.class));
			if (null == pbRewardInfoDto) {
				throw new ServiceException(null, "查询不到相关信息");
			}
			PbRewardInfoForm pbRewardInfoForm = BeanHelper.copyProperties(pbRewardInfoDto, PbRewardInfoForm.class);
			request.setAttribute("pbRewardInfoForm", pbRewardInfoForm);
            request.setAttribute("rewardOid", rewardOid);
		} catch (Exception se) {
			this.handleException(request, se, "rewardOid=" + rewardOid);
			return mapping.getInputForward();
		}
		return mapping.findForward("success");
	}

	/**
	 * 跳转到 PbRewardInfo信息添加页面
	 * 
	 * @param mapping
	 * @param form
	 * @param request
	 * @param response
	 * @return
	 * @throws Exception
	 */
	public ActionForward goCreate(ActionMapping mapping, ActionForm form, HttpServletRequest request, HttpServletResponse response) throws Exception {
		//参数校验，可根据实际情况修改
		try
		{
			String personOid = request.getParameter("personOid");
			String urlId = request.getParameter("urlId");
			if(StringUtils.isEmpty(personOid))
			{
				throw new ServiceException("personOid is null");
			}
			request.setAttribute("personOid", personOid);
			request.setAttribute("urlId", urlId);
		}
		catch(Exception se)
		{
			this.handleException(request, se, null);
			return mapping.getInputForward();
		}
		return mapping.findForward(FORWARD_SUCCESS);
	}

	/**
	 * 添加 PbRewardInfo信息
	 * @param mapping
	 * @param form
	 * @param request
	 * @param response
	 * @return
	 * @throws Exception
	 */
	public ActionForward create(ActionMapping mapping, ActionForm form, HttpServletRequest request, HttpServletResponse response) throws Exception {
		if (this.isCancelled(request)) {
			return mapping.findForward("cancel");
		}
		String personOid = request.getParameter("personOid");
		PbRewardInfoForm pbRewardInfoForm = (PbRewardInfoForm) form;
		try {
			if(StringUtils.isEmpty(personOid))
			{
				throw new ServiceException("personOid is null");
			}
			PbRewardInfoDTO pbRewardInfoDto = BeanHelper.copyProperties(pbRewardInfoForm, PbRewardInfoDTO.class);
			pbRewardInfoDto.setPersonOid(NumberUtils.longValue(personOid));
			pbRewardInfoDto.setCreateBy(UserContext.getLoginUserID());
			pbRewardInfoDto.setCreateName(UserContext.getLoginUserName());
			pbRewardInfoDto.setCreateDate(DateUtil.now());
			pbRewardInfoFacade.create(pbRewardInfoDto);
			response.getWriter().write(JSONHelper.fromObject(true, null).toString());
		} catch (Exception se) {
			this.handleException(request, se, null);
			response.getWriter().write(JSONHelper.fromObject(false, StringUtils.defaultIfEmpty(se.getMessage(), "修改失败")).toString());
		}

		return null;
	}


	/**
	 * 跳转到 PbRewardInfo信息修改（编辑）页面前的查询回显
	 * 
	 * @param mapping
	 * @param form
	 * @param request
	 * @param response
	 * @return
	 * @throws Exception
	 */
	public ActionForward goUpdate(ActionMapping mapping, ActionForm form, HttpServletRequest request, HttpServletResponse response) throws Exception {
		if(this.isCancelled(request)) {
			return mapping.findForward("cancel");
		}

		String urlId = request.getParameter("urlId");
        String rewardOid = request.getParameter("rewardOid");

		try {
            if (StringUtils.isBlank(rewardOid)) {
				throw new ServiceException("error.pk.invalid", "pbRewardInfo rewardOid is null");
			}
            PbRewardInfoDTO pbRewardInfoDto = pbRewardInfoFacade.get(ObjectUtil.getValue(rewardOid, java.lang.Long.class));
			if (null == pbRewardInfoDto) {
				throw new ServiceException("error.object.notfound", "pbRewardInfo not found by rewardOid");
			}
			PbRewardInfoForm pbRewardInfoForm = BeanHelper.copyProperties(pbRewardInfoDto, PbRewardInfoForm.class);
			request.setAttribute("pbRewardInfoForm", pbRewardInfoForm);
			request.setAttribute("urlId", urlId);
            request.setAttribute("rewardOid", rewardOid);
		} catch (Exception se) {
			this.handleException(request, se, "rewardOid=" + rewardOid);
			return mapping.getInputForward();
		}
		return goUpdateView(mapping, form, request, response);
	}

	/**
	 * 跳转到 PbRewardInfo信息修改（编辑）页面
	 * 
	 * @param mapping
	 * @param form
	 * @param request
	 * @param response
	 * @return
	 * @throws Exception
	 */
    public ActionForward goUpdateView(ActionMapping mapping, ActionForm form, HttpServletRequest request, HttpServletResponse response) throws Exception {
        //add jsp init values
        @SuppressWarnings("unused")
		PbRewardInfoForm s = (PbRewardInfoForm) form;
        return mapping.findForward("success");
    }

	/**
	 * 更新 PbRewardInfo信息
	 * 
	 * @param mapping
	 * @param form
	 * @param request
	 * @param response
	 * @return
	 * @throws Exception
	 */
	public ActionForward update(ActionMapping mapping, ActionForm form, HttpServletRequest request, HttpServletResponse response) throws Exception {
		if(this.isCancelled(request)) {
			return mapping.findForward("cancel");
		}
		
		PbRewardInfoForm pbRewardInfoForm = (PbRewardInfoForm) form;
		try {

			PbRewardInfoDTO pbRewardInfoDto = new PbRewardInfoDTO();
			BeanHelper.copyProperties(pbRewardInfoForm, pbRewardInfoDto);
			pbRewardInfoDto.setUpdateBy(UserContext.getLoginUserID());
			pbRewardInfoDto.setUpdateName(UserContext.getLoginUserName());
			pbRewardInfoDto.setUpdateDate(DateUtil.now());
			pbRewardInfoFacade.update(pbRewardInfoDto);
			response.getWriter().write(JSONHelper.fromObject(true, null).toString());
		} catch (Exception se) {
			this.handleException(request, se, pbRewardInfoForm);
			response.getWriter().write(JSONHelper.fromObject(false, StringUtils.defaultIfEmpty(se.getMessage(), "修改失败")).toString());
		}

		return null;
	}



	/**
	 * 删除 PbRewardInfo信息
	 * 
	 * @param mapping
	 * @param form
	 * @param request
	 * @param response
	 * @return
	 * @throws Exception
	 */
    public ActionForward delete(ActionMapping mapping, ActionForm form, HttpServletRequest request, HttpServletResponse response) throws Exception {
           String rewardOid = request.getParameter("rewardOid");
        try {
           if(StringUtils.isEmpty(rewardOid))
			{
				throw new ServiceException(null, "rewardOid is null");
			}
            pbRewardInfoFacade.delete(ObjectUtil.getValue(rewardOid, java.lang.Long.class));
            response.getWriter().write(JSONHelper.fromObject(true, null).toString());
        } catch (Exception se) {
            this.handleException(request, se, "rewardOid=" + rewardOid);
            return mapping.getInputForward();
        }
        return null;
    }

}
