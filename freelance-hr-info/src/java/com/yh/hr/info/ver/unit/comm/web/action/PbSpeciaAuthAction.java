/**
 * @desctiption   This file is generated by  code generation tool version 0.2 ^_^
 * @Created   2017-02-13
**/
package com.yh.hr.info.ver.unit.comm.web.action;

import jade.workflow.utils.ObjectUtil;

import java.util.Map;

import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

import org.apache.commons.beanutils.BeanUtils;
import org.apache.commons.lang.StringUtils;
import org.apache.struts.action.ActionForm;
import org.apache.struts.action.ActionForward;
import org.apache.struts.action.ActionMapping;

import com.yh.component.taglib.TableTagBean;
import com.yh.hr.info.ver.unit.comm.facade.PbSpeciaAuthFacade;
import com.yh.hr.info.ver.unit.comm.web.form.PbSpeciaAuthForm;
import com.yh.hr.res.pb.dto.PbSpeciaAuthDTO;
import com.yh.platform.core.exception.ServiceException;
import com.yh.platform.core.util.BeanHelper;
import com.yh.platform.core.util.DateUtil;
import com.yh.platform.core.util.JSONHelper;
import com.yh.platform.core.util.NumberUtils;
import com.yh.platform.core.util.SpringBeanUtil;
import com.yh.platform.core.util.StringUtil;
import com.yh.platform.core.web.UserContext;
import com.yh.platform.core.web.action.BaseAction;

public class PbSpeciaAuthAction extends BaseAction {
	private PbSpeciaAuthFacade pbSpeciaAuthFacade = (PbSpeciaAuthFacade) SpringBeanUtil.getBean("pbSpeciaAuthFacade");

	/**
	 * 列出所有 PbSpeciaAuth信息
	 * 
	 * @param mapping
	 * @param form
	 * @param request
	 * @param response
	 * @return
	 * @throws Exception
	 */
	public ActionForward listPbSpeciaAuthInfo(ActionMapping mapping, ActionForm form, HttpServletRequest request, HttpServletResponse response) throws Exception {
		TableTagBean ttb = TableTagBean.getFromRequest(request);
		if (request.getMethod().equals("POST")) {
	        @SuppressWarnings("rawtypes")
			Map map = BeanUtils.describe(form);
	        ttb.getCondition().copyFrom(map, true);
	    } else {
	        BeanUtils.populate(form, ttb.getCondition());
	        request.setAttribute("pbSpeciaAuthForm", form);
	    }
        try {
	        pbSpeciaAuthFacade.find(ttb);
	        request.setAttribute("list", ttb.getList());
        } catch (Exception se) {
			this.handleException(request, se, null);
			return mapping.findForward("fail");
		}
        return mapping.findForward("success");
	}
	
	/**
	 * 查找单个PbSpeciaAuth信息
	 * 
	 * @param mapping
	 * @param form
	 * @param request
	 * @param response
	 * @return
	 * @throws Exception
	 */
	public ActionForward show(ActionMapping mapping, ActionForm form, HttpServletRequest request, HttpServletResponse response) throws Exception {
        String speciaAuthOid = request.getParameter("speciaAuthOid");
        if (StringUtil.isBlank(speciaAuthOid)) {
            speciaAuthOid = ObjectUtil.getValue(request.getAttribute("speciaAuthOid"), String.class);
        }
		try {
            if (StringUtils.isEmpty(speciaAuthOid)) {
				throw new ServiceException("error.pk.invalid", "pbSpeciaAuth speciaAuthOid is null");
			}
            PbSpeciaAuthDTO pbSpeciaAuthDto = pbSpeciaAuthFacade.get(ObjectUtil.getValue(speciaAuthOid, java.lang.Long.class));
			if (null == pbSpeciaAuthDto) {
				throw new ServiceException(null, "查询不到相关信息");
			}
			PbSpeciaAuthForm pbSpeciaAuthForm = BeanHelper.copyProperties(pbSpeciaAuthDto, PbSpeciaAuthForm.class);
			request.setAttribute("pbSpeciaAuthForm", pbSpeciaAuthForm);
            request.setAttribute("speciaAuthOid", speciaAuthOid);
		} catch (Exception se) {
			this.handleException(request, se, "speciaAuthOid=" + speciaAuthOid);
			return mapping.getInputForward();
		}
		return mapping.findForward("success");
	}

	/**
	 * 跳转到 PbSpeciaAuth信息添加页面
	 * 
	 * @param mapping
	 * @param form
	 * @param request
	 * @param response
	 * @return
	 * @throws Exception
	 */
	public ActionForward goCreate(ActionMapping mapping, ActionForm form, HttpServletRequest request, HttpServletResponse response) throws Exception {
		//参数校验，可根据实际情况修改
		String urlId = request.getParameter("urlId");
		String personOid = request.getParameter("personOid");
		try
		{
			if(StringUtils.isEmpty(personOid))
			{
				throw new ServiceException("personOid is null");
			}
			request.setAttribute("personOid", personOid);
			request.setAttribute("urlId", urlId);
		}
		catch(Exception se)
		{
			this.handleException(request, se, null);
			return mapping.getInputForward();
		}
		return mapping.findForward(FORWARD_SUCCESS);
	}

	/**
	 * 添加 PbSpeciaAuth信息
	 * @param mapping
	 * @param form
	 * @param request
	 * @param response
	 * @return
	 * @throws Exception
	 */
	public ActionForward create(ActionMapping mapping, ActionForm form, HttpServletRequest request, HttpServletResponse response) throws Exception {
		if (this.isCancelled(request)) {
			return mapping.findForward("cancel");
		}
		String personOid = request.getParameter("personOid");
		PbSpeciaAuthForm pbSpeciaAuthForm = (PbSpeciaAuthForm) form;
		try {
			if(StringUtils.isEmpty(personOid))
			{
				throw new ServiceException("personOid is null");
			}
			PbSpeciaAuthDTO pbSpeciaAuthDto = BeanHelper.copyProperties(pbSpeciaAuthForm, PbSpeciaAuthDTO.class);
			pbSpeciaAuthDto.setPersonOid(NumberUtils.longValue(personOid));
			pbSpeciaAuthDto.setCreateBy(UserContext.getLoginUserID());
			pbSpeciaAuthDto.setCreateName(UserContext.getLoginUserName());
			pbSpeciaAuthDto.setCreateDate(DateUtil.now());
			pbSpeciaAuthFacade.create(pbSpeciaAuthDto);
			response.getWriter().write(JSONHelper.fromObject(true, null).toString());
		} catch (Exception se) {
			this.handleException(request, se, null);
			response.getWriter().write(JSONHelper.fromObject(false, StringUtils.defaultIfEmpty(se.getMessage(), "修改失败")).toString());
		}

		return null;
	}


	/**
	 * 跳转到 PbSpeciaAuth信息修改（编辑）页面前的查询回显
	 * 
	 * @param mapping
	 * @param form
	 * @param request
	 * @param response
	 * @return
	 * @throws Exception
	 */
	public ActionForward goUpdate(ActionMapping mapping, ActionForm form, HttpServletRequest request, HttpServletResponse response) throws Exception {
		if(this.isCancelled(request)) {
			return mapping.findForward("cancel");
		}

        String speciaAuthOid = request.getParameter("speciaAuthOid");
        String urlId = request.getParameter("urlId");

		try {
            if (StringUtils.isBlank(speciaAuthOid)) {
				throw new ServiceException("error.pk.invalid", "pbSpeciaAuth speciaAuthOid is null");
			}
            PbSpeciaAuthDTO pbSpeciaAuthDto = pbSpeciaAuthFacade.get(ObjectUtil.getValue(speciaAuthOid, java.lang.Long.class));
			if (null == pbSpeciaAuthDto) {
				throw new ServiceException("error.object.notfound", "pbSpeciaAuth not found by speciaAuthOid");
			}
			PbSpeciaAuthForm pbSpeciaAuthForm = BeanHelper.copyProperties(pbSpeciaAuthDto, PbSpeciaAuthForm.class);
			request.setAttribute("pbSpeciaAuthForm", pbSpeciaAuthForm);
            request.setAttribute("speciaAuthOid", speciaAuthOid);
            request.setAttribute("urlId", urlId);
		} catch (Exception se) {
			this.handleException(request, se, "speciaAuthOid=" + speciaAuthOid);
			return mapping.getInputForward();
		}
		return goUpdateView(mapping, form, request, response);
	}

	/**
	 * 跳转到 PbSpeciaAuth信息修改（编辑）页面
	 * 
	 * @param mapping
	 * @param form
	 * @param request
	 * @param response
	 * @return
	 * @throws Exception
	 */
    public ActionForward goUpdateView(ActionMapping mapping, ActionForm form, HttpServletRequest request, HttpServletResponse response) throws Exception {
        //add jsp init values
        return mapping.findForward("success");
    }

	/**
	 * 更新 PbSpeciaAuth信息
	 * 
	 * @param mapping
	 * @param form
	 * @param request
	 * @param response
	 * @return
	 * @throws Exception
	 */
	public ActionForward update(ActionMapping mapping, ActionForm form, HttpServletRequest request, HttpServletResponse response) throws Exception {
		if(this.isCancelled(request)) {
			return mapping.findForward("cancel");
		}
//        String speciaAuthOid = request.getParameter("speciaAuthOid");
		PbSpeciaAuthForm pbSpeciaAuthForm = (PbSpeciaAuthForm) form;
		try {
//            if (StringUtils.isBlank(speciaAuthOid)) {
//				throw new ServiceException("error.pk.invalid", "pbSpeciaAuth speciaAuthOid is null");
//			}
//			if(StringUtils.isEmpty(personOid))
//			{
//				throw new ServiceException("personOid is null");
//			}
			PbSpeciaAuthDTO pbSpeciaAuthDto = new PbSpeciaAuthDTO();
			BeanHelper.copyProperties(pbSpeciaAuthForm, pbSpeciaAuthDto);
			pbSpeciaAuthDto.setUpdateBy(UserContext.getLoginUserID());
			pbSpeciaAuthDto.setUpdateName(UserContext.getLoginUserName());
			pbSpeciaAuthDto.setUpdateDate(DateUtil.now());
			pbSpeciaAuthFacade.update(pbSpeciaAuthDto);
			response.getWriter().write(JSONHelper.fromObject(true, null).toString());
		} catch (Exception se) {
			this.handleException(request, se, pbSpeciaAuthForm);
			response.getWriter().write(JSONHelper.fromObject(false, StringUtils.defaultIfEmpty(se.getMessage(), "修改失败")).toString());
		}

		return null;
	}



	/**
	 * 删除 PbSpeciaAuth信息
	 * 
	 * @param mapping
	 * @param form
	 * @param request
	 * @param response
	 * @return
	 * @throws Exception
	 */
    public ActionForward delete(ActionMapping mapping, ActionForm form, HttpServletRequest request, HttpServletResponse response) throws Exception {
           String speciaAuthOid = request.getParameter("speciaAuthOid");
        try {
           if(StringUtils.isEmpty(speciaAuthOid))
			{
				throw new ServiceException(null, "speciaAuthOid is null");
			}
            pbSpeciaAuthFacade.delete(ObjectUtil.getValue(speciaAuthOid, java.lang.Long.class));
            response.getWriter().write(JSONHelper.fromObject(true, null).toString());
        } catch (Exception se) {
            this.handleException(request, se, "speciaAuthOid=" + speciaAuthOid);
            return mapping.getInputForward();
        }
        return null;
    }

}
